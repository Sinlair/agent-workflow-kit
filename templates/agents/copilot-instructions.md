# Copilot Instructions

Use `AGENTS.md` as the project source of truth for commands, conventions, and review expectations.

Prefer small changes that match nearby code and include tests when behavior changes.
